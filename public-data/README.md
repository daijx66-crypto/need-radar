# Generated public data

Files in this directory are sanitized, derived outputs produced by
`scripts/build_public.py`. They intentionally exclude raw responses, caches,
private profiles, feedback, local paths, and internal evaluation data.

The daily GitHub workflow may update `needs.json`, `status.json`, and the
30-day compact history after the quality gate passes.
